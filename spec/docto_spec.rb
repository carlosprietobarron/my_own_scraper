require 'docto'

RSpec.describe 'docto' do
    it 'open the site smashingmag.com returning a doc object' do
        pending("something else getting finished")
        this_should_not_get_executed
    end

    it 'stores article item in the elements array' do
        pending("something else getting finished")
        this_should_not_get_executed
    end

    it 'returns each of the article items formated as text \
        from the elements array' do
        pending("something else getting finished")
        this_should_not_get_executed
    end

    it 'returns all the article items formated as html from \
        the elements array' do
        pending("something else getting finished")
        this_should_not_get_executed   
    end

end